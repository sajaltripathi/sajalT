<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardRoot"
    style="@style/Widget.SajalT.CardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/space_sm">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="@dimen/space_sm">

        <TextView
            android:id="@+id/tvPageNumber"
            android:layout_width="28dp"
            android:layout_height="wrap_content"
            android:text="1"
            android:textStyle="bold"
            android:textColor="@color/on_surface_muted"
            android:gravity="center" />

        <ImageView
            android:id="@+id/ivThumbnail"
            android:layout_width="@dimen/thumbnail_size"
            android:layout_height="@dimen/thumbnail_size"
            android:layout_marginStart="@dimen/space_sm"
            android:scaleType="centerCrop"
            android:background="@color/surface_variant"
            android:contentDescription="@string/cd_thumbnail" />

        <TextView
            android:id="@+id/tvFileName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/space_sm"
            android:maxLines="2"
            android:ellipsize="middle"
            android:textAppearance="@style/TextAppearance.SajalT.Body"
            android:textSize="13sp" />

        <ImageButton
            android:id="@+id/btnMoveUp"
            android:layout_width="@dimen/control_touch_target"
            android:layout_height="@dimen/control_touch_target"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_up"
            android:contentDescription="@string/imgtopdf_move_up" />

        <ImageButton
            android:id="@+id/btnMoveDown"
            android:layout_width="@dimen/control_touch_target"
            android:layout_height="@dimen/control_touch_target"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_down"
            android:contentDescription="@string/imgtopdf_move_down" />

        <ImageButton
            android:id="@+id/btnRemove"
            android:layout_width="@dimen/control_touch_target"
            android:layout_height="@dimen/control_touch_target"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_close"
            android:contentDescription="@string/imgtopdf_remove" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
